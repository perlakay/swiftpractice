import UIKit

var a:Int = 5

var b:[Int] = [5, 10, 15, 20, 25] //define an array with values
//what data type is an array?
//array is its own data type that stores a specific data type indicated by: varaiblename:[data type of data stored] = [values]

print(a) //call a
print(b[0]) //calling a specific index of the array, remember arrays start at 0

print(b[4]) //calls last item

//order is preserved in arrays

//how do you assign new data?
b[0] = 100 //assign new value to specific index

print(b[0])

